/**
 * CUSTOM HOOKS
 * =============
 * Optimized React hooks for the centrifuge simulator
 */

export {
  useSmoothedValues,
  useTrendData,
  useAlarms,
  useSimulationLoop,
  useDebugPanel,
} from './useSimulation';
